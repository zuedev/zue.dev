{
  "scripts": {
    "dev": "wrangler dev",
    "dev:remote": "wrangler dev --remote",
    "deploy": "wrangler deploy --minify"
  },
  "devDependencies": {
    "@cloudflare/puppeteer": "^1.0.4",
    "wrangler": "^4.45.3"
  }
}
